FROM node:18

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

# Build the app
RUN npm run build

# Install static server
RUN npm install -g serve

EXPOSE 5173

# Serve built files
CMD ["serve", "-s", "dist", "-l", "5173"]